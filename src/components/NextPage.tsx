

const NextPage: React.FC = (page) => {

  const handleClickNext: React.FC = () => {
    if (page >= 1) {
      page++;
      console.log(page);
    }
  };

  return (
  )
};

export default NextPage;
